# 避震器馬達控制整合記錄

## 背景 / 目的

原本馬達控制邏輯（鎖緊/中間/全鬆三段式避震器調整）跑在獨立的 ESP32 上（Arduino
`0709_3btn_edge.ino`），經過大量實測才調出可靠的堵轉偵測 + 動態歸零（過程中燒壞過一顆馬達）。
這次把整套邏輯移植進樹莓派本身的 Python 專案 `System_Integration`，直接用 Pi 5 的 GPIO 驅動，
不再需要 ESP32：主程式（`main.py`）已經在跑路面辨識、人車偵測、車門偵測，馬達動作直接依這些
既有的辨識結果自動觸發，不需要實體按鈕。

### 版本沿革
1. 最初在 USB 備份（`/media/beau/0E01-3576/System_Integration`）上開發，基於 commit `99ad044`（car_door 整合），未 commit
2. 2026-09-25 移植到桌面版（已含 `df67344` IMX219 原生解析度），README 衝突以桌面版的 1280×960 為準；
   移植時改了兩處：lgpio 改走系統套件、main.py 馬達忙碌提示不洗版（見下方表格）
3. 2026-09-25 上機測試後修正：脈衝濾波改用 lgpio tick、兩端都轉到死點並動態歸零、`pos_mid` / `pos_loose` 依實測行程重設、
   校正時印出漂移、新增寸動工具 `tests/manual_jog.py`（見「關鍵設計決策」與「測試狀態」）

---

## 新增的檔案

| 檔案 | 內容 |
|---|---|
| `configs/motor.yaml` | 馬達所有可調參數：GPIO 腳位、脈衝濾波、三個目標位置、煞車時間、堵轉偵測門檻、坑洞代理門檻、位置存檔路徑、防抖動參數 |
| `src/motor.py` | `MotorController`：用 `lgpio` 驅動 M+/M-、讀編碼器脈衝，移植 Arduino 的雙模式堵轉偵測 + 動態歸零（上機後改成全緊、全鬆兩端都有）+ 位置持久化 |
| `src/motor_policy.py` | `decide_target()`（依路面/坑洞/人員/車門自動決定目標位置）+ `TargetDebouncer`（防抖動，仿 `road_type.yaml` 的 `confirm_count`） |
| `tests/simulate_motor.py` | 不需要真實硬體的模擬測試，假 `lgpio` + 背景執行緒模擬轉動的馬達，驗證整個決策/狀態機邏輯。執行：`uv run tests/simulate_motor.py` |
| `tests/manual_jog.py` | 寸動工具：每次只轉 0.1～0.5 秒，把避震器手動轉回全緊、重設位置存檔；印有效脈衝累計（跟 `src/motor.py` 同一套 tick 濾波）與脈衝時間診斷，用來實測 `pos_loose` |
| `tests/manual_gpio_test.py` | 上機測試階段 1：空轉（不掛避震器），鍵盤手動控制正反轉 + 看脈衝數，只測 GPIO 接線，不牽涉堵轉/歸零邏輯 |
| `tests/manual_motor_control.py` | 上機測試階段 2：裝上避震器後，鍵盤手動觸發 `move_to()`，用真正的 `MotorController`（含堵轉偵測、動態歸零、位置持久化），還不接 CV 自動決策 |

## 修改的既有檔案

| 檔案 | 改了什麼 |
|---|---|
| `src/settings.py` | 加 `MotorSettings` dataclass（21 個欄位）、加進 `Settings`、`load_settings()` 讀 `motor.yaml` 並驗證（腳位不重複、位置範圍、比例值 > 0 等） |
| `pyproject.toml` | 註解說明 `lgpio` 走系統套件 `python3-lgpio`（PyPI 版只有原始碼、缺 swig 編不起來，不列進 `dependencies`） |
| `main.py` | import 馬達相關模組；建立並啟動 `motor` 執行緒；主迴圈每幀呼叫 `decide_target()` → `TargetDebouncer` → 冷卻時間檢查 → `move_to()`；狀態列加馬達顯示；收尾流程加 `motor.stop()`/`join()`/`close()`；確認的新目標遇到馬達忙碌時，「命令被略過」同一個目標只印一次（USB 版每幀都印，20 fps 下每秒 20 行） |
| `src/event_log.py` | `FIELDS` 加 `"motor"`，`snapshot()` 多吃一個 `motor_status` 參數，記錄目標位置名稱（tight/mid/loose） |
| `README.md` | 加「避震器馬達控制」章節（決策規則、防抖動、位置持久化、時間精度提醒、已知訊號落差）；模組表、config 表、log 欄位表、`presets/` 說明都補上馬達相關項目 |

---

## 關鍵設計決策

### 為什麼用 `lgpio` 不是 `RPi.GPIO`
Pi 5 是 RP1 晶片，`RPi.GPIO` 原生不支援。`lgpio` 是 Pi 5 官方建議的做法，`callback()` 的回呼
帶有核心層時間戳記（tick，64-bit 奈秒，不會回繞）。

### 脈衝間隔用 lgpio 的 tick，不用回呼時間（2026-09-25 上機後修正）
lgpio 把邊緣事件排進 pipe，再由通知緒一批一批呼叫 Python 回呼。原本 `_on_pulse()` 用
`time.perf_counter()`（回呼被呼叫的時間）算間隔，同一批送進來的真脈衝間隔幾乎是 0，被 150 µs 濾波
當雜訊丟掉。`tests/manual_jog.py` 實測：同樣轉回全緊，tick 濾波往鬆 65 / 往緊 65（對稱），
回呼時間濾波只算到 43 / 44，約丟三分之一，所以 ESP32 校的 `pos_mid` / `pos_loose` 在 Pi 上會多走約 1.5 倍。
現在濾波與比例堵轉的基準間隔改用 tick；「距上次脈衝多久」（堵轉判斷）仍用 `perf_counter()`，
因為要跟現在時間比，回呼延遲只有幾毫秒。每次移動的第一個脈衝沒有前一個 tick 可比，只收下不設基準值
（原本會把「起步到第一個脈衝」當第一個基準），起步保護交給 `start_timeout_ms`。

### 全緊、全鬆兩端都轉到頂住死點才停，並動態歸零（2026-09-25 上機後改，跟 Arduino 版不同）
Arduino 版只有全緊端確認過是機構死點，所以只在全緊歸零，而且計數到目標就停。上機實測後改成：

- `tests/manual_jog.py` 實測全鬆端（約 -197）也是死點：再往鬆轉完全沒脈衝，所以兩端都可信任拿來當校正基準
- 計數兩個方向有約 10% 漂移（同一段行程往鬆算 197、往緊算 178）。計數到目標就停的話，漂移方向一反過來，
  往全緊會在計數到 0 時停在死點前 → **鎖不緊**（第一次上機測 3 → 1 就遇到）。「有人 / 車門開 → 鎖緊」是安全規則，不能靠計數
- 所以目標是全緊或全鬆時**不看計數，一律轉到時間差比例堵轉**才停，再把位置校正成 `pos_tight` / `pos_loose`；
  `move_timeout_ms`（5 秒，全程實測約 3 秒）是最後一道保護。代價是每次到兩端都頂住約 `min_stall_floor_us`（100 ms）+ 煞車 100 ms
- 中間位置沒有死點，照計數停、用簡單版堵轉偵測當安全網；誤差只累積「從某一端走到中間」這一段（約 10 個脈衝）
- 一啟動就沒脈衝（`stall_start`）證據較弱（也可能是馬達/編碼器故障），不歸零，跟 Arduino 版相同
- 校正時 log 印出校正前的計數與漂移，例如 `位置自動校正：-8 → 0（漂移 -8）`（全緊端負值 = 還沒算到 0 就頂到）。
  在車上長時間跑時看這個數字：穩定在同一範圍就正常，越來越大或突然跳很大代表計數有問題

### 兩層防抖動，避免頻繁切換磨損齒輪
`decide_target()` 是每幀重算的瞬時決策，路面標籤、坑洞格數、人員偵測在臨界點附近都可能一幀
一幀跳。這次額外加了兩層保護（Arduino 版原本沒有，這是 auto-trigger 架構新引入的風險，Arduino
是按鈕觸發不會有這個問題）：

1. `confirm_count`（預設 5）：同一個目標要連續這麼多次決策都一樣才算確認（`TargetDebouncer`）
2. `min_switch_interval_sec`（預設 3 秒）：就算目標確認變了，離上次真的送出切換命令沒過這麼久
   也先不送——切換頻率的硬上限，在 `main.py` 主迴圈實作

### 位置持久化（比 Arduino 版更完整）
樹莓派有真正的檔案系統，不需要 ESP32 那種 NVS。每次移動完成後把位置存進
`presets/motor_position.json`（先寫暫存檔、再 `os.replace()` 改名覆蓋，atomic write），開機時
讀回來，不用像 Arduino 版每次開機都假設全緊。讀不到/壞掉/超出範圍時才退回 `pos_tight`。

---

## 已知的訊號落差（需要你確認，還沒有最終定案）

這兩點是這次自動決策架構特有的近似值，不是原本 Arduino 版按鈕觸發時要考慮的問題：

1. **「坑洞」= severe 格數代理，不是真的坑洞偵測**
   `src/grading/pothole/detector.py` 的 `PotholeDetector` 是死程式碼，沒有被接進
   `CementGrader`/`AsphaltGrader` 的流程。目前用「畫面 severe 格數達門檻」代理，門檻在
   `motor.yaml` 的 `pothole_severe_cell_threshold`（預設 2）調整。要更準的話需要另外把
   `PotholeDetector` 接進分級流程。

2. **「人員移動」= 有偵測到 person 類別，不是真的判斷速度**
   `TrackView`（`src/detect.py`）沒有速度欄位，只有座標歷史。目前簡化成「畫面裡偵測到 person
   就觸發鎖緊」，不判斷是否真的在動。人站著不動一樣會觸發。

---

## 已知限制：移動中不能被新命令中斷（2026-09-25 記錄，先不改，之後有需要再處理）

`move_to()` 在馬達忙碌時直接拒絕新命令（沿用 Arduino 的 `motorBusy` guard），任何移動都會走完才接受下一個；
`main.py` 被拒絕後每幀重試，移動結束才送得出去。只有程式結束（`stop()`）會中途停下（`aborted`）。

影響：安全規則（有人 / 車門開 → 鎖緊）會被延遲。例如剛開始往全鬆（全程約 3 秒）就偵測到人，要先走完全鬆、
再從全鬆轉回全緊，**最壞約 6 秒才鎖緊**；另外 `min_switch_interval_sec`（3 秒）對鎖緊也一樣適用。

討論過的改法（未實作）：
- 只讓「鎖緊」可以插隊：先煞車停下目前的移動，再反轉去全緊（全緊一律頂到死點校正，中斷那一趟的計數誤差會被修正），
  最壞情況縮到約 3 秒；其他目標照舊排隊
- 鎖緊不受 `min_switch_interval_sec` 限制
- 代價：轉動中煞車再反轉，對齒輪 / 馬達衝擊較大

---

## 已修正的 bug

**`move_to()` 跟 `_execute_move()` 之間的競態窗口**：原本 `busy` 標記是背景執行緒真正開始跑
`_execute_move()` 時才設 `True`，命令排進佇列後、執行緒還沒真正醒來處理前的空檔，如果又呼叫一次
`move_to()`，會直接覆寫掉還沒執行的命令。已修正為 `move_to()` 一確認不忙碌就立刻標記忙碌，
兩者合併成一個原子操作。

---

## 測試狀態

### ✅ 已用模擬驗證（`tests/simulate_motor.py`，23 項全過）
模擬馬達有自己的真實位置與兩端死點，跟程式算的位置分開，才能模擬計數漂移。
- 開機讀回/預設位置
- 中間位置正常移動到位（含 tolerance 判斷）
- 兩端堵轉 + 動態歸零（計數往兩個方向漂移 5 格，都會頂到死點後校正回 `pos_tight` / `pos_loose`）
- 鎖不緊的回歸測試：計數到 0 時實際還差 5 格，仍要轉到頂住全緊（USB 舊版停在實際 -11）
- 忙碌中命令正確被拒絕（驗證了上面那個 bug 修正）
- 回呼成批送達時不漏算脈衝（舊版回呼時間濾波在這個情境送出 90 只算到 18）
- 位置持久化（存檔 → 重新建立 controller → 讀回）
- `decide_target()` 四條規則的覆蓋順序
- `TargetDebouncer` 防抖動（純抖動不確認 / 連續穩定才確認）

### ✅ 已在 Pi 5 上驗證（2026-09-25）
- `lgpio` 的 API 呼叫方式（`gpiochip_open(0)` 是 `pinctrl-rp1`、`gpio_claim_alert()`、`callback()`、`.cancel()`）
- 轉向：M+（程式的正轉）= 鎖緊，腳位不用對調
- 行程：全緊 0 → 全鬆死點約 -197（tick 濾波後的有效脈衝），全程約 3 秒；起步到第一個脈衝 1～61 ms，
  轉動中脈衝最長間隔 57 ms。`motor.yaml` 改為 `pos_mid: -98`、`pos_loose: -197`（原 ESP32 值 -280 / -560 超過行程）

- 階段 2（`tests/manual_motor_control.py`，裝上避震器、Hailo 沒在跑）：3 → 1 → 2 → 3 → 2 → 1 全部正確——
  兩端都是 `stall` 並校正到 -197 / 0，沒有半路誤判堵轉、沒有 timeout / stall_start；中間兩次停在 -97 / -99；
  使用者目視兩端「應該是」鎖緊 / 全鬆；重新執行讀回 pulse = 0（位置持久化正常）。
  堵轉門檻（`min_stall_floor_us` 100 ms 等）沿用 ESP32 值，在這個條件下沒問題

### ❌ 尚未在真實硬體驗證
- 階段 3：`main.py` 同時跑 Hailo 推論、相機、錄影時，堵轉判斷的「多久沒脈衝」會不會被系統負載拖慢而誤判
- 車上實際負載下，快鎖緊時脈衝間隔會不會超過 100 ms（會的話會在死點前被判堵轉，要調高 `min_stall_floor_us`）
- 從全緊 / 全鬆過來的中間位置，實際差多少（計數有約 10% 漂移，估計約 10 個脈衝）

---

## 建議的下一步（分三階段上機測試）

進度（2026-09-25）：階段 1、2 已完成，下一步是階段 3。

1. **階段 1：空轉測試**（✅ 完成：轉向正確、編碼器有訊號）（不掛避震器）—— `uv run tests/manual_gpio_test.py`
   鍵盤手動控制正反轉、看脈衝數，確認接線方向對不對、編碼器有沒有正常回饋，完全繞開
   `src/motor.py` 的堵轉/歸零邏輯，純粹測 GPIO 接線
2. **階段 2：裝上避震器後，手動切換測試**（✅ 完成，結果見「測試狀態」） —— `uv run tests/manual_motor_control.py`
   改用真正的 `MotorController`（鍵盤輸入 1/2/3 觸發 `move_to("tight"/"mid"/"loose")`），驗證
   堵轉偵測、全緊動態歸零、位置持久化在真實負載下準不準，還不接 CV 自動決策。這一步如果發現
   堵轉門檻太緊或太鬆，回頭調 `configs/motor.yaml`（先保守起跑、確認沒問題再逐步放寬，跟
   Arduino 版當初的調校方式一樣）
3. **階段 3：自動辨識** —— 前兩階段都沒問題後，直接 `uv run main.py`，`decide_target()` 會
   依路面/坑洞/人員/車門自動觸發，不用再寫額外的測試腳本。第一次跑人要在旁邊看；
   注意 log 的漂移值、有沒有 timeout，以及切換頻率（`TargetDebouncer` 是逐幀計數，`confirm_count` 5 約 0.25 秒，
   加上 `min_switch_interval_sec` 3 秒；太頻繁再調）
4. 決定要不要處理已知的兩個訊號落差（接上真正的坑洞偵測、加人員移動速度判斷）
5. 長時間穩定性測試（README 建議至少 10~15 分鐘），確認 Hailo 推論負載下馬達控制不會卡頓
   或誤判堵轉
